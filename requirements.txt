# Core FL framework
nvflare>=2.5.0,<2.7.0

# Deep learning (install torch matching your CUDA/arch separately if needed,
# e.g. for aarch64 + Blackwell: pip install torch --index-url https://download.pytorch.org/whl/cu128)
torch>=2.4.0
transformers>=4.40.0
tokenizers>=0.19.0
huggingface-hub>=0.23.0

# Data: Therapeutics Data Commons (molecular datasets)
# NOTE: pinned <1.0 — PyTDC >=1.0 pulls in `tiledb`, which has no prebuilt
# aarch64 wheel and fails to build from source on ARM (e.g. GB10/DGX Spark).
# On x86_64 you may relax this pin. Install with: pip install --no-deps PyTDC==0.4.1
PyTDC>=0.4.1,<1.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0
rdkit>=2023.9.1

# Scientific stack
numpy>=1.24,<2.2
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0

# Quasi-Monte-Carlo sampling for coverage estimation (scipy.stats.qmc is used; this is a fallback)
# Config / CLI / utils
pyyaml>=6.0
tqdm>=4.66
